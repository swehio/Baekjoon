#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int cnt = 0;

    while(1)
    {
        string word;
        cin >> word;
        if(cin.eof()) break;

        cnt++;
    }

    cout << cnt;
}